import { RouteLoading } from '@/components/layout/route-loading';

const Loading = () => {
  return <RouteLoading title="Loading post" description="Pulling in the article, metadata, and reading view." />;
};

export default Loading;
