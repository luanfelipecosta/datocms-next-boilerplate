import { RouteLoading } from '@/components/layout/route-loading';

const Loading = () => {
  return <RouteLoading title="Loading site" description="Warming up the next section and assembling the layout." />;
};

export default Loading;
